/*#include"train.h"
#include<iostream>
#include <bits/stdc++.h>
#include <gtest/gtest.h>

using namespace std;

TEST(Train,DefaultConstructor) {
    Train trn;
    EXPECT_EQ(0,trn.currentTrack());
    EXPECT_EQ(0,trn.upcomingTrack());
}
int main(int argc, char **argv)
    {
    testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
    }
*/
